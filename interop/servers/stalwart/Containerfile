# Stalwart interop target. The image's default bootstrap configuration exposes
# the SMTP listener and stores mail under /opt/stalwart/data/mail. The profile
# supplies a deterministic recovery-admin credential for diagnostics; the SMTP
# smoke transaction itself is deliberately unauthenticated.
FROM docker.io/stalwartlabs/stalwart
